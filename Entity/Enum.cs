namespace Feedback.Entity
{
    public enum TicketStatus
    {
        Open,
        Closed,
        InProgress
    }
    public enum OpinionStatus
    {
        Pending,
        InProgress,
        Completed
    }
}
